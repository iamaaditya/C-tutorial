#include <stdio.h>
#include <string.h>


struct Student {
    char name[100];
    char email[100];
    char address[100];
    int id;
};


int main() {
    
    char name[100000] = "James Storer, Brandeis University";

    struct Student s1;
    struct Student *sp;

    s1.
    sp->

    strcpy(s1.name, "James Storer");
    /* s1.name = ""; */
    /* s1.address = ""; */
    /* s1.email = ""; */

    printf("%s", name);

    return 0;

}
